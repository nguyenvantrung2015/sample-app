<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items.feed_by_time %>
  </ol>
  <%= paginate @feed_items, theme: "twitter-bootstrap-4", pagination_class:
      "pagination-sm" %>
<% end %>
