<% provide :title, t("user.index.all_user") %>
<h1><% t "user.index.all_user" %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>
<%= paginate @users, theme: "twitter-bootstrap-4", pagination_class:
    "pagination-sm" %>
